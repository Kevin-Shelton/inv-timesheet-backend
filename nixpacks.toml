providers = ["python"]

[phases.install]
cmds = [
  'python -m venv /opt/venv',
  '/opt/venv/bin/pip install -r requirements.txt'
]

[start]
cmd = "/opt/venv/bin/gunicorn --bind 0.0.0.0:$PORT --timeout 120 src.main:app"

